"""Removed: static sorter was removed per user request.

This file previously contained a script to statically sort gallery cards. The
script was removed on {date}. If you need a sorter again, re-create the
script or ask to restore it from the repository history.
""".format(date='2025-09-16')
